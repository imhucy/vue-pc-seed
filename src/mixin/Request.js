// 依赖mixin/Page
// request会和页面上的
// import Request from 'util/Request'
export default {}
/*
inject: ['$page'],
  methods: {
    $request (name, params) {
      // let $page = this.$page
      // if ($page) {
      //   let {
      //     // 初始化接口名称
      //     init_requests
      //   } = $page.$page_info
      // }
      // let promise = this.$store.dispatch(name, params)
      // init_requests[name].count++
      // return promise
    }
  }
*/
